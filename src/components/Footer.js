import React from "react";

import "./Footer.css";

const Footer = (props) => {
  return (
    <footer>
      <input className="input" type="text"></input>
    </footer>
  );
};

export default Footer;
